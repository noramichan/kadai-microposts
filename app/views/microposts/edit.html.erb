<h1>id: <%= @micropost.id %> のメッセージ編集ページ</h1>

<%= form_with(model: @micropost, local: true) do |f| %>
  <%= f.label :content, 'メッセージ' %>
  <%= f.text_field :content %>

  <%= f.submit '投稿' %>
<% end %>

<%= link_to '一覧に戻る', microposts_path %>